# ドメイン文書

この区分は、ffmpeg-docker を別のビルドシステムへ置き換えても必要になる
外部知識を管理します。

## 文書一覧

- [FFmpeg ソースと RTMP 派生機能](ffmpeg-sources.md): upstream FFmpeg と
  aoirint/FFmpeg の関係、バージョン参照、`rtmp_strict_paths` の挙動を説明します。

upstream のリリース系列、フォークのパッチ、公開オプションが変わったときに
この区分を更新します。
